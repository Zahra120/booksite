extends ../application.pug
block content

	.container
		.row
			each book in books
				.col-xs-3
					a(href="/book/"+ book.id)
						img(width="225" height="335" src=book.imageURL)
						h3= book.title
						h5=book.author
						p=book.description
